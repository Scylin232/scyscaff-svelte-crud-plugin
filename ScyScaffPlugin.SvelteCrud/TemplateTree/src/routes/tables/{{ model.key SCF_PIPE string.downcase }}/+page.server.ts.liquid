import { PUBLIC_{{ model_entity_name | string.upcase }}_SERVICE_PORT } from '$env/static/public';
import { tableLoad } from '$lib/tableLoad';

export async function load({ fetch, url }) {
	return await tableLoad(fetch, url, "{{ model.key }}", "{{ model_entity_name | string.downcase }}", PUBLIC_{{ model_entity_name | string.upcase }}_SERVICE_PORT);
}