<script lang="ts">
  import { PUBLIC_{{ model_entity_name | string.upcase }}_SERVICE_PORT } from '$env/static/public';
  import Table from '../../../components/tables/Table.svelte';

	export let data;

	const {{ model.key | string.downcase }}Model = {
		id: 'protected',
		createdAt: 'protected',
		updatedAt: 'protected',
    {{~ for field in model.value ~}}
        {{ field.key | string.downcase }}: '',
    {{~ end ~}}
	};
</script>

<Table data={data} model={%{{}%}{{ model.key | string.downcase }}Model{%{}}%} modelName="{{ model.key }}" servicePort={%{{}%}PUBLIC_{{ model_entity_name | string.upcase }}_SERVICE_PORT{%{}}%} />