<script lang="ts">
	export let model: object;
	export let isAdding: boolean;
</script>

{#if isAdding}
	{#each Object.keys(model) as modelKey}
		<td class="whitespace-nowrap px-3 py-4 text-sm text-gray-500">
			<input name={`add_${modelKey}`}
						 placeholder={modelKey}
						 disabled={model[modelKey] === "protected"}
						 type="text"
						 class="p-2 text-sm text-gray-900 border border-gray-300 rounded-lg bg-gray-50 disabled:bg-gray-200 focus:ring-indigo-500 focus:border-indigo-500" />
		</td>
	{/each}

	<td class="relative whitespace-nowrap py-4 pl-3 pr-4 text-right text-sm font-medium sm:pr-6">
		<button type="submit" class="text-indigo-600 hover:text-indigo-900" value="Add">Submit</button>
	</td>
{/if}